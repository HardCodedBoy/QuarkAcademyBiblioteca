Por favor, especifica tu nombre completo: BRUNO RIBA

1) En C#, ¿para qué sirve una propiedad?.
-Principalmente nos sirve para poder mantener el principio de encapsulación en nuestro código, ya que nos proporcionan
herramientas para poder obtener el valor de un campo de una clase y asignar un valor a un miembro de la clase usando 
una lógica específica. 

2) ¿Cuándo utilizaría acceso protegido en los miembros de una clase?.
-Cuando quiera que los miembros de una clase sean solo accesibles en las clases que extiendan a la clase que aplique
los miembros protegidos.

3)En UML, ¿qué tipo de relación es la siguiente?
Realización.

3.5)Explique de qué trata dicha relación.
-Indica que una clase está obligada a implementar los métodos, con sus correspondientes "signatures", de la interfaz. Además,
la clase que es obligada a implementar la interfaz, debe definir el cuerpo de los métodos que impuestos.

4)Explique con sus palabras qué implica una relación de Dependencia entre dos clases.
-Es una relación que declara que un elemento utiliza los servicios y datos de otro elemento, pero no necesariemente a la inversa.

Indique V o F según corresponda. Si es F, fundamente su respuesta:
a)Un constructor es un método que se invoca de forma automática cuando se instancia el objeto de la clase.
-Verdadero

b)Un constructor debe tener siempre el mismo nombre de la clase. 
-Verdadero

c)Un constructor puede retornar un valor.
-Falso.  El constructor jamás retornará un valor, pues para lo que se usa es asignar valores determinados a una instancia de una clase.
 
d)Un constructor puede ser privado.
-Verdadero

e)Una clase sólo puede tener declarado un único constructor. 
-Falso. Una clase puede tener múltiples constructores declarados. Es importante destacar que estos deben diferenciarse en su "signature".



-----------------------------------
INSTRUCCIONES:
-El programa consiste en una interfaz que permite realizar ciertas tareas específicas.
-El ID de los usuarios se asigna por orden de inscripción a la biblioteca
-La estantería se asigna automáticamente. El sistema asigna la primera estantería libre que encuentra.
-Se debe ingresar SI O SI un ISBN correcto cada vez que se pida, caso contrario el sistema arrojará un error.
